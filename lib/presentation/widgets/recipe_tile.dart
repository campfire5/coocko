import 'package:flutter/material.dart';

class CoockoRecipeTile extends StatelessWidget {
  const CoockoRecipeTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
